name: barcodereader
description: A new flutter plugin project.
version: 0.0.1
author: Attalli Ayoub <attalliayoub50@gmail.com>
homepage: https://github.com/AttalliAyoub/barcodereader.git

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  animations: ^2.0.0
  camera: ^0.8.1
  screen: ^0.0.5
  # scan: ^1.4.3
  # zxing2: ^0.1.0

  rxdart:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.ayoub.barcodereader
        pluginClass: BarcodereaderPlugin